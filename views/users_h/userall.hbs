<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>전체 유저 조회</title>
<link rel="stylesheet" href="/css/default.css">
</head>
<body>
    
    {{#each user}}
        <div id="page">
            <form action="/users/findAllUser" method="get"></form>
            <p>userId : {{this.userId}}</p>
            <p>email : {{this.email}}</p>
            <p>nickname : {{this.nickname}}</p>
            <p>address : {{this.address}}</p>
            <p>createdAt : {{this.createdAt}}</p>
            <p>updatedAt : {{this.updatedAt}}</p>
            </form>
            <button onclick="window.location.href = '/users/users_h/userEdit';">유저 정보 수정하기</button>
            <button onclick="window.location.href = '/users/users_h/userDelete';">유저 정보 삭제하기</button>
        </div>
        {{/each}}
        <a href="/">Home</a>
</body>
</html>